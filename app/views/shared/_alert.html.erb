<% if flash.present? %>
  <div class="container mx-auto mt-4 z-40">
    <% flash.each do |msg_type, msg| %>
      <% if msg.is_a?(String) %>
        <%= flash_alert(msg_type, msg) %>
      <% end %>
    <% end %>
  </div>
<% end %>
