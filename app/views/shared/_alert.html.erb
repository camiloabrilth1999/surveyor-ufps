<% if flash.present? %>
  <div class="container mx-auto z-40">
    <% flash.each do |msg_type, msg| %>
      <% if msg.is_a?(String) %>
        <%= flash_alert(msg_type, msg) %>
      <% end %>
    <% end %>
  </div>
<% end %>
